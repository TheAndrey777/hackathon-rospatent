<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RospatentHackathon.MainPage">

    <ContentPage.Resources>
        <x:Double x:Key="rotationAngle">-15</x:Double>
        <x:Double x:Key="fontSize">24</x:Double>
        <OnPlatform x:Key="textColor"
                    x:TypeArguments="Color">
            <On Platform="iOS" Value="Red" />
            <On Platform="Android" Value="Aqua" />
            <On Platform="WinUI" Value="#80FF80" />
        </OnPlatform>
        
        <Style TargetType="Button" x:Key="MyStyle">
            <Setter Property="HorizontalOptions" Value="Center" />
            <Setter Property="VerticalOptions" Value="Center" />
            <Setter Property="BorderWidth" Value="3" />
            <Setter Property="Rotation" Value="{StaticResource rotationAngle}" />
            <Setter Property="TextColor" Value="{StaticResource textColor}" />
            <Setter Property="FontSize" Value="{StaticResource fontSize}" />
        </Style>
    </ContentPage.Resources>

    <StackLayout>
        <AbsoluteLayout HeightRequest="50"/>
        <Button Text="Do this!" Style="{StaticResource MyStyle}"/>
        <Button Text="Do that!" />
        <Button Text="Do the other thing!" />
    </StackLayout>

</ContentPage>
